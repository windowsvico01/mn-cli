#!/usr/bin/env node

console.log('测试mn-cli')

// const command = process.argv[2];
// const Webpack = require('webpack');
// const WebpackDevServer = require('webpack-dev-server');

// const defaultDevConfig = {};
// const defaultProdConfig = {};

// if (command === 'dev') {
//   console.log('测试----dev');
//   const compiler = Webpack(defaultDevConfig);
//   const devServerOptions = defaultDevConfig.devServer;
//   const devServer = new WebpackDevServer(compiler, devServerOptions);
//   devServer.listen(8080, 'localhost', () => console.log('Start server on http://localhost:8080'));
// } else if (command === 'build') {
//   console.log('测试----build');
//   Webpack(defaultProdConfig);
// } else {
//   console.log('测试----default')
// }